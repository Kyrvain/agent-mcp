# Agent MCP CDP Web Console

Vue 3 + Vite + TypeScript + Element Plus frontend for the local FastAPI backend.

```powershell
npm install
npm run dev
```

The dev server proxies `/api` to `http://127.0.0.1:8000`.
